describe("API тесты - Проверка сервера кинотеатра", () => { it("1. Проверка доступности главной страницы клиента (GET)", () => { cy.request("GET", "http://qamid.tmweb.ru").then((response) => { expect(response.status).to.eq(200); }); }); it("2. Проверка доступности страницы админки (GET)", () => { cy.request("GET", "http://qamid.tmweb.ru").then((response) => { expect(response.status).to.eq(200); }); }); });
